package org.algorithm_datastructure;

import java.util.Arrays;
import java.util.HashMap;

public class token_bucket_rate_limiter {

    public static void main(String[] args) {

        // bucket that stores token, pre-defined capacity
        // bucket stores token until it gets full
        // each request consume one token
        // if token exist requests will be processed, unless will be dropped
        // need to consider the refill rate which is the number of tokens put periodically into bucket

        // bucket per endpoint per user for example 2 user and 3 endpoint we need 3 bucket per user



    }


    private static void tokenBucketRateLimiter() {



    }


}